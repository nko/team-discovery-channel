
<div class="outer-box">
  <div class="outer-box-inset">
    <div class="inner-box">
      <div class="inner-box-inset">
        <p style="margin-bottom:1em">
          Our hyper intelligent and self-aware cloud servers have analyzed and run your unit tests. Results are below!
        </p>
        
        <p>
          To set up a post-commit hook for your GitHub repo, <a id="web-hook-toggle" href="#">click here</a>. We'll even notify you when your build breaks!
        </p>
        
        <div style="clear:both"></div>
      </div>
    </div>
  </div>
</div>

<div class="outer-box" id="webhook" style="display:none">
  <div class="outer-box-inset">
      <label for="test-url">Twitter Handle</label>
      <input id="twitter-handle" type="input" name="twitter-handle" />
      <label style="margin-top: 5px"  for="hook">GitHub Hook</label>
      <input id="hook" type="input" name="hook" value=""/>
      <p>
        To set up a post-commit hook for your GitHub repo, <a id="create-github-link" href="#">click here</a>. We'll even notify you when your build breaks!
      </p>
      
      <div style="clear:both"></div>
  </div>
</div>


<% for (var index = 0; index < test_results.rows.length; index++) { %>
<% results = test_results.rows[index].value %>

<div class="outer-box">
  <div class="outer-box-inset">
    <div class="header test-result-toggle">
      <a class="toggle-open" href="#"></a>
      <div class="title">c3dd5f... <%= results.date %></div>
      <div class="score"> 80 / 84</div>
    </div>
    
    <div class="inner-box" style="display:none">
      <div class="inner-box-inset">
        <ul class="tests">
        <% for (var i = 0, test; (test = results.output[i]) != null; i++) { %>

            <li class="header"><%= test.name %></li>

            <% for (var j = 0, assertion; (assertion = test.tests[j]) != null; j++) { %>

                <% if (assertion.result) { %>
                    <li class="passed"><%= assertion.message %></li>
                <% } else { %>
                    <li class="failed"><%= assertion.message %></li>
                <% } %>

            <% } %>
        <% } %>
        </ul>
      </div>
    </div>
  </div>
</div>
<% } %>
